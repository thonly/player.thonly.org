const template = document.createElement("template");

template.innerHTML = `
    <link rel="stylesheet" href="components/to-audio/shadow.css">
    <audio controls preload loop></audio>
`;

export default template;